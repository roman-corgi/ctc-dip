<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
            elementFormDefault="unqualified"
            attributeFormDefault="unqualified">
  <!-- defines how the recipe files are defined on disk for override -->
  <xs:complexType name="recipe_item">
    <xs:annotation>
      <xs:documentation xml:lang="en">
        Define what is needed for each of the recipe items.
      </xs:documentation>
    </xs:annotation>
    <xs:simpleContent>
      <xs:extension base="xs:string">
        <xs:attribute name="location" type="xs:normalizedString" use="required"/>
      </xs:extension>
    </xs:simpleContent>
  </xs:complexType>

  <xs:complexType name="recipe_type">
    <xs:annotation>
      <xs:documentation xml:lang="en">
        Define all of the recipe files.
      </xs:documentation>
    </xs:annotation>
    <xs:all>
      <xs:element default="no comment" name="l1_eng_a.recipe" type="recipe_item"/>
      <xs:element default="no comment" name="l2a_eng_a.recipe" type="recipe_item"/>
    </xs:all>
  </xs:complexType>

  <xs:element name="recipes">
    <xs:complexType>
      <xs:annotation>
        <xs:documentation xml:lang="en">
          This terrible construct is so that xjc can autodetect this as the
          root node for processing. Many things would be better but this is
          the most workable solution especially if the making of the binding
          code is automated in the pom. The only other real solution is to
          modify one of the classes generated by hand.
        </xs:documentation>
      </xs:annotation>
      <xs:complexContent>
        <xs:extension base="recipe_type"/>
      </xs:complexContent>
    </xs:complexType>
  </xs:element>
</xs:schema>
